import CreateId from "../components/CreateId";

type PostsPageProps = {
    setValidId: React.Dispatch<React.SetStateAction<string[]>>;
};

export function UsersPage({ setValidId }: PostsPageProps) {
    return (
        <></>
    );
}
